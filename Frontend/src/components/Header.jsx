import { Bell, Search, UserRound } from "lucide-react";
const titles = { dashboard: "Traffic overview", map: "Live traffic map", predictions: "Predictions", alerts: "System alerts" };
export default function Header({ currentTab, cities, activeCity, onSelectCity, searchQuery, setSearchQuery, onAdminAccess }) {
  return 
  (<header className="sticky top-0 z-40 min-h-20 px-8 flex items-center justify-between gap-6 border-b border-outline-variant bg-surface-container-low/90 backdrop-blur-md"><div className="flex items-center gap-7 min-w-0"><div><h2 className="text-lg font-bold">{titles[currentTab]}</h2><p className="text-xs text-on-surface-variant">Real-time city operations</p></div><div className="hidden lg:flex gap-1">{cities.map(city => <button key={city.id} onClick={() => onSelectCity(city)} className={`px-3 py-1.5 rounded-full text-xs cursor-pointer ${activeCity?.id === city.id ? "bg-primary/10 text-primary font-bold" : "text-on-surface-variant"}`}>{city.name}</button>)}</div></div><div className="flex items-center gap-3"><div className="relative"><Search className="absolute left-3 top-2.5 text-on-surface-variant" size={15}/><input value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search areas" className="w-44 sm:w-56 rounded-full border border-outline-variant bg-white py-2 pl-9 pr-3 text-xs outline-none focus:border-primary"/></div><Bell size={18} className="text-on-surface-variant"/><button onClick={onAdminAccess} className="w-9 h-9 grid place-items-center rounded-full bg-primary/10 text-primary cursor-pointer"><UserRound size={18}/></button></div></header>);
}
