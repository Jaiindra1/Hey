import { TrendingUp } from "lucide-react";
export default function PredictionsView({ city }) { return <div className="space-y-6"><section className="grid gap-5 sm:grid-cols-3"><Card label="Active reroutes" value={city.activeReroutingCount}/><Card label="Prediction accuracy" value={`${city.reroutingAccuracy}%`}/><Card label="Detected anomalies" value={city.anomalyCount}/></section><section className="glass-card rounded-2xl p-6"><div className="flex gap-3 items-center"><TrendingUp className="text-primary"/><div><h3 className="font-bold">Load prediction</h3><p className="text-sm text-on-surface-variant">Expected network load over the next hour</p></div></div><div className="mt-7 space-y-5">{city.criticalNodes.map(node => <div key={node.id}><div className="flex justify-between gap-4 text-sm"><div><strong>{node.name}</strong><span className="ml-2 text-xs text-on-surface-variant">{node.type}</span></div><span className="text-primary">{node.predictedLoad}% predicted</span></div><div className="mt-2 h-3 overflow-hidden rounded-full bg-surface-container"><div className="h-full bg-secondary" style={{width:`${node.currentLoad}%`}}/><div className="h-full -mt-3 bg-primary/55" style={{width:`${node.predictedLoad}%`}}/></div></div>)}</div></section></div>; }
function Card({label,value}) { return (<div className="glass-card rounded-2xl p-5"><p className="text-xs uppercase tracking-wider text-on-surface-variant">{label}</p><strong className="mt-3 block text-3xl text-primary">{value}</strong></div>); }
