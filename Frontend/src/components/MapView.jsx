import { MapPin, Navigation } from "lucide-react";
export default function MapView({ city, searchQuery }) { const zones = city.zones.filter(zone => zone.name.toLowerCase().includes(searchQuery.toLowerCase())); return (<div className="glass-card overflow-hidden rounded-2xl"><div className="flex justify-between p-6"><div><h3 className="font-bold">{city.name} traffic map</h3><p className="mt-1 text-sm text-on-surface-variant">Live network flow · {city.lat}, {city.lng}</p></div><span className="rounded-full bg-secondary-container px-3 py-1 text-xs font-bold text-secondary">{city.flowIndex}% flowing</span></div><div className="map-grid min-h-105 p-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{zones.map((zone, index) => <div key={zone.name} className="self-center rounded-xl border border-outline-variant bg-white/85 p-4 shadow-sm" style={{transform:`translateY(${index % 2 ? 20 : 0}px)`}}><div className="flex gap-2 items-center text-primary"><MapPin size={18}/><strong className="text-sm">{zone.name}</strong></div><p className="mt-3 text-xs text-on-surface-variant">Efficiency</p><div className="mt-1 flex justify-between items-center"><strong className="text-2xl">{zone.efficiency}%</strong><Navigation size={17} className="text-secondary"/></div></div>)}</div></div>); }
